import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    listItemContainer: {
        margin: 8,
        padding: 5,
        justifyContent: 'center',
        borderRadius: 15,
        elevation: 8,
      },
});
