import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listItemContainer: {
    marginHorizontal: 16,
    marginVertical: 12,
    justifyContent: 'center',
    borderRadius: 16,
    elevation: 1
  }
});
