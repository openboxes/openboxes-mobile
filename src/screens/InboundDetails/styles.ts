import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerTitle: {
    fontWeight: 'bold',
    backgroundColor: 'white',
    margin: 10,
    padding: 5,
    fontSize: 16,
    borderRadius: 10,
    elevation: 8
  }
});
