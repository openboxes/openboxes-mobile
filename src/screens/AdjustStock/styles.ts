import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputSpinner: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 30
  }
});
