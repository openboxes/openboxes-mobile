import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  countLabelAndIconContainer: {
    display: 'flex',
    flexDirection: 'row',
    flex: 0,
    alignItems: 'center'
  }
});
