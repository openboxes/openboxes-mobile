import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    marginVertical: 5,
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center'
  },
  textInput: {
    flex: 1,
  },
  imageIcon: {
    bottom: -15,
  }
});
